apiVersion: v1
kind: Service
metadata:
  name: {{ include "biblio.fullname" . }}
  labels:
    {{- include "biblio.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "biblio.selectorLabels" . | nindent 4 }}
