apiVersion: v1
kind: Service
metadata:
  name: {{ include "biblio-backend.fullname" . }}
  labels:
    {{- include "biblio-backend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    {{- include "biblio-backend.selectorLabels" . | nindent 4 }}
